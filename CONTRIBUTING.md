# Contributing

Be precise about privacy guarantees. Any speech provider must disclose whether processing is local or remote, and transcript mutations must remain inspectable.

Run `npm run check` and `npm audit` before opening a pull request.
